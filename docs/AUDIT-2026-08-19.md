# CredentialDOMD Audit: 2026-08-19

## 1. Headline

18 confirmed P1s, zero P0s. Three clusters: the invite gate can be self-defeated, the tax numbers you act on are wrong by 9 to 14 percent on your own data, and sync silently loses edits, deletes, and at least one of your files. The SEO mystery (2,560 impressions, 1 click) is solved: 44 of 50 state pages ship literal "[RESEARCH NEEDED]" into Google snippets and 17 contradict your own verified CME database. Claude can fix everything below alone except two items needing your confirmation.

## 2. Fix now (all P1)

1. **Beta gate bypass.** Any public sign-up can PATCH its own `access_status` to active and unlock the shared Gemini key. Affects invite-only integrity and your Google bill. Fix: revoke UPDATE on the four sensitive profile columns and extend the identity-lock trigger. Claude: yes.
2. **S-corp estimate overstates tax $2,691 (9%) live.** Salary clamp drops the employer-payroll and franchise deduction. Affects your Sept 15 quarterly. Fix: pass the entity loss through K-1, floor AGI instead, surface the clamp in a note. Claude: yes.
3. **QBI never computed below the phase-out.** Federal ~$5,300 high at your year-end trajectory; the existing note implies it was modeled. Affects any S-corp or sole-prop user and their CPA. Fix: add the 199A threshold constants, linear phase-down, render the deduction line. Claude: yes.
4. **Cat 1 counted by regex.** CO shows you 0/30 when 14.75 AMA hours count; a CA DO is falsely told the AOA minimum is met. Fix: explicit per-state `cat1Accepted` data, regex only as fallback, pin with tests. Claude: yes.
5. **One-time mandates re-demanded every cycle.** CA pain management 12 hrs, all six CT topics (Fowler tracks CT), WA, FL, GA. Fix: `period` field per topic (lifetime or years:N) and branch in compliance.js. Claude: yes.
6. **Reminder emails silently off.** `notify_email` NULL is excluded from the digest while the UI shows the toggle ON; live for the invited physician today, and "Skip setup" recreates it for every new user. Fix: backfill plus column defaults, treat NULL as opted-in server-side, persist defaults on skip. Claude: yes.
7. **Offline or failed edits and deletes silently revert.** Only additions self-heal; one transient 401 loses the change. Fix: durable pending-ops queue replayed before merge. Claude: yes.
8. **Delete All My Data resurrects.** No tombstones, so any other device's cache re-uploads everything; the hand-built reset also crashes locum adds. Fix: tombstone all ids on wipe, reset from DEFAULT_DATA, paginate storage cleanup. Claude: yes.
9. **Backup restore drops 13 collections** (invoices, work log, deductibles, tax payments) and reports success. Fix: derive the import whitelist from TABLE_MAP, add a round-trip test. Claude: yes.
10. **Your career case-log master .xlsx points at a missing storage object.** The local copy was likely evicted; monthly backups skip it. Fix: repair the reconcile branches and re-upload; if the cache is gone you re-export the Google Sheet. Claude: code yes, file recovery may need you.
11. **~78MB main-thread stringify after every edit**, and the offline cache goes silently stale once documents hydrate. Fix: strip document bytes from the cache write. Claude: yes.
12. **Backups bucket converges to ~3x each corpus** and the prune orphans physical files; 1GB breaks at 4 or 5 document-heavy users. Fix: prune via the storage API, keep one month. Claude: yes.
13. **Invite email gives false instructions**: own-key claim (untrue and a privacy misstatement), magic link, a checklist that no longer appears. Fix: rewrite inviteText, redeploy. Claude: yes.
14. **profiles.email is frozen while five screens say fix it in Settings.** Inbound matching, reminders, backups, and packet reply-to all key on it; 3 of 5 profiles are blank. Fix: allow owner email updates (keep auth_user_id frozen) or a verified-change flow; align all copy. Claude: yes; the policy choice is yours.
15. **Leftover ANMG webhook on the shared Resend account** receives CredentialDOMD bounce and complaint events. Fix: delete it and add your own bounce handler with suppression. Claude: after you confirm ANMG is off this account.
16. **No DMARC, no apex SPF.** Spoofable founder address and worse deliverability to hospital gateways. Fix: one `_dmarc` TXT (p=quarantine, relaxed alignment) plus apex `v=spf1 -all`. Claude: yes, via the Cloudflare API.
17. **The two state-page P1s** (placeholders in snippets and schema; 17 states contradicting stateRequirements.js) are items 1 and 2 of the SEO plan below. Claude: yes.

## 3. Discoverability

1. Make generate.js placeholder-safe: null fees instead of the false "$0" HowTo cost, drop placeholder FAQs from JSON-LD, "See board site" in quick facts, regenerate. Effect: fixes the broken SERP snippet on 44 pages including Maryland, your top-impression state, and removes a structured-data spam risk. Ships today with zero research.
2. Derive CME hours, cycle, and topics from stateRequirements.js (already primary-source verified 2026-08-17). Effect: kills all 17 contradictions, including three states shown with CME they do not require.
3. Link the homepage to /states/: nav, footer, and a section linking the hub plus Maryland, Texas, California, Ohio, Pennsylvania. Effect: internal PageRank finally reaches the pages getting impressions; cheapest lever at average position 19.7.
4. Extensionless internal links plus a Cloudflare 301 for /states/*.html. Effect: consolidates link equity currently split across two live URLs per page.
5. Numbers-first titles and meta: "{State} Medical License Renewal 2026: {fee} Fee, {hours} CME, {month} Deadline". Effect: CTR is 1/2560; this is the snippet rewrite that earns clicks at position 15 to 20.
6. Add depth from data you already own: MD vs DO subsections for the 13 split states, topic-mandate lists, fee tables, a visible "Verified August 2026" line. Effect: content competitors lack, plus the untouched "{state} osteopathic license renewal" keyword set.
7. Research the 44 incomplete states in states-data.json, Maryland and Ohio first.
8. Housekeeping: stop deploying generate.js and states-data.json, fix the sitemap/robots conflict on /app/ pages, add BreadcrumbList and ItemList JSON-LD.

## 4. Next two weeks (P2 by theme)

**Authorization hardening:** revoke broad default DML grants on admin and service tables; unify ADMIN_EMAILS with app_admins; fix the legacy user_events policy.
**Keys and secrets:** purge the device key slot on sign-out and delete; restrict the legacy key-adoption scan (cross-account key inheritance, two findings); add api.anthropic.com to the CSP so the own-Anthropic-key feature works at all; shorten the 35-day backup bearer link; raise the secretBox minimum.
**Money:** statement-import "also bill" double-dip, the one error in the underpayment direction; set-aside excludes employer payroll (~$10.8k/yr cash gap); reconcile memo vs engine meals totals.
**Compliance engine:** CME tab's lifetime AOA card; import and scan pickers missing state-mandated topics; MATE satisfied by generic pain CME; window boundary timezone bug; surface the ND MOC exemption you qualify for.
**Sync and durability:** transient read failures treated as empty (stale overwrite); backup retention status filter; three missing DEFAULT_DATA keys; shareLog sharedAt column mismatch; split the client/server notification columns and honor snooze before the Aug 28 ack lapse; fetch document bytes on demand instead of the 58MB reconcile; delta sync.
**Performance:** code-split xlsx, jspdf, and CPT catalogs (807KB gz to ~520); vendor chunking; default shared-key traffic to flash with a spend ceiling.
**Second physician:** de-neurosurgeon the Vera, dictation, and CPT prompts; gate Locum UI on actual locum data, not tier; align FAQ and Vera with the shared-key reality; dual-issuer clerkAuth so the Clerk cutover is order-independent (sequencing stays yours); relay loop guard; enable no-use-before-define lint and move the two risky declarations.

## 5. Verified fine

Tenant isolation is uniform and correct across all user tables and storage; app_secrets genuinely service-role-only; no secrets in bundle or git history. All five real work invoices, the ANMG duty invoice, income-by-state, forecast dedupe, and 2026 federal constants reproduce exactly. Transcript PDF matches the compliance card; MATE lifetime counting, item snoozes, and cycle anchoring are right. Webhooks signature-verified; inbound idempotent with layered anti-abuse; packet email hardened. Monthly backup covers all 30 tables and ran clean. Canonicals, sitemap, JSON-LD skeleton, and page speed are sound. Assistant token discipline is good.

## 6. Refuted

- Daily reminder-email loop: refuted; the client wipe never reaches the DB, resend requires user interaction, and zero emails have ever sent. Real column collision remains, downgraded P2.
- Free-tier egress outage from document reconcile: refuted; the project is on Pro, ~20GB of 250GB. Waste is real, downgraded P2.
- is_admin depending on auth.users: stale; it reads app_admins correctly.
## 7. Shipped (2026-08-19)

The four-cluster fix workflow ran and each branch was independently review-verified before merge (MERGE verdict on compliance and sync, MERGE-WITH-FIXES on tax; the one flagged copy fix was applied). All live on `credentialdomd.com`:

- **Tax** (merge d084b3f): S-corp entity payroll + franchise now pass through as a K-1 loss instead of vanishing (AGI floored at 0); QBI/199A SSTB phase-down implemented (`QBI_SSTB_THRESHOLD` added); half-SE deduction excludes the non-deductible 0.9% additional Medicare; `cashReserveRate` includes employer payroll; statement-import "bill to agency" rows go to travelExpenses only (double-dip closed). Verified numerically against live founder data.
- **Compliance** (merge 3802180): `STATE_REQS` gained `period` (lifetime / {years:N}) and `cat1Accepted` markers so one-time and periodic mandates are machine-readable; hour values and `verified` markers unchanged. 31/31 `scripts/compliance.test.mjs`.
- **Sync** (merge aab43df): durable per-account replay queue for failed writes (edits/deletes no longer silently revert); a failed read is kept as last-known-good and excluded from self-heal (no stale overwrite); timestamp-based last-write-wins; `COLLECTION_KEYS` derived from `TABLE_MAP` (verified exact parity); sign-out clears the device-key slot. `deleted_items` table already present.
- **bkemail**: no change needed.

**SEO relaunch** (merge 98e92fe): all 51 jurisdictions (50 states + new DC page) rebuilt from primary board sources — real deadlines, fees, CME totals, board portal URLs, sources; 25 double-verified, 26 single-source (flagged). Numbers-first titles/meta, MD/DO subsections on split states, extensionless internal links matching the canonical, homepage nav + footer + discovery section linking to `/states/`, sitemap refreshed. `renewalInfo.js` and `send-reminders/renewalLinks.json` regenerated from the same data; **send-reminders redeployed to v5** with the fresh links.

**Migrations:** `20260819_lock_access_status`, `20260819_identity_lock_relink`, `20260819_prune_client_errors` all applied and verified live (`lock_profile_identity()` freezes access_status for users with a service_role carve-out; `prune-client-errors` cron scheduled).

**Forward-looking caveat:** the deduction double-dip fix is forward-only. Deductibles already saved from prior "also bill" imports remain double-counted; retroactive cleanup is a data task, not done.

**Still on Eric:** Clerk production cutover (add webhook + secret key, then "keys are in"); move `itinerary6168090517457973106.pdf` into `~/Projects` or upload in-app for the Travel section (macOS blocks Downloads access).
